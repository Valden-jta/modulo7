/**
 * AddCollection
 *
 * Página para crear una nueva colección de libros.
 *
 * TODO:
 * - Añadir formulario con nombre, descripción y visibilidad (pública/privada).
 * - Integrar selector de libros existentes para añadir a la colección.
 * - Conectar con la tabla `collection` y `collection_book` en la BBDD.
 */
function AddCollection() {
  return <div>AddCollection</div>;
}

export default AddCollection;
