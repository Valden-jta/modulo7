/**
 * FavoritesCollection
 *
 * Página para la colección especial de favoritos.
 *
 * TODO:
 * - Definir cómo se marca un libro como favorito (modelo `user_book.rating` o flag dedicado).
 * - Mostrar los libros favoritos del usuario agrupados en una colección virtual.
 * - Permitir reordenar o filtrar dentro de esta colección.
 */
function FavoritesCollection() {
  return <div>FavoritesCollection</div>;
}

export default FavoritesCollection;
