/**
 * CollectionPage
 *
 * Página principal de colecciones del usuario.
 *
 * TODO:
 * - Listar todas las colecciones del usuario.
 * - Mostrar número de libros por colección y estado (pública/privada).
 * - Permitir navegar a detalles y edición de cada colección.
 */
function CollectionPage() {
  return <div>CollectionPage</div>;
}

export default CollectionPage;
