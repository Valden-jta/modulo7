/**
 * SocialGroups
 *
 * Página para la gestión de grupos de lectura.
 *
 * TODO:
 * - Listar grupos a los que pertenece el usuario.
 * - Permitir crear y administrar grupos propios.
 * - Mostrar información básica de cada grupo (miembros, últimos hilos).
 * - Conectar con el modelo `group` y `group_member`.
 */
function SocialGroups() {
  return <div>SocialGroups</div>;
}

export default SocialGroups;
